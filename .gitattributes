# This removes Jupyter Notebook outputs from the committed version of the file
# the filter it uses (with the actual command) is defined in the repo's .git/config
# *.ipynb filter=strip-notebook-output

# Automatically include archive metadata in releases
.git_archival.txt  export-subst